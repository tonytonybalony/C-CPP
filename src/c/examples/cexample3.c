#include <stdio.h>

int main() {
    printf("This is C example file 3\n");
    
    // Example: Array
    int arr[5] = {1, 2, 3, 4, 5};
    for (int i = 0; i < 5; i++) {
        printf("arr[%d] = %d\n", i, arr[i]);
    }
    
    return 0;
}